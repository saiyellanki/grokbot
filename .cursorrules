# Scratchpad

## Current task
Improve ACBN synthetic PoC for examiner-ready continuous controls monitoring.

[X] Vault event schema: policy_version, collector_id, control_ids, env
[X] Exception aging module + fixture + HITL + out artefact
[X] SOC use-case library (privilege-host KEV / jump-host) + attestation
[X] Dual-control HITL token schema + reuse reject + sample out
[X] Redaction policy stub (deny unredacted NPI/CHD)
[X] Docs: README + grok-e2e pass criteria
[X] run_poc.py exits 0; chain_ok; unauthorized_prod_mutations==0
[X] Branch, commit, PR

## Measured (SYNTHETIC run 2026-09-10)
- chain_ok true; unauthorized_prod_mutations 0
- exception_breached 2 / aging 1 / HITL +3
- soc_collector_coverage 1.0; detection_attested_rate 0.0
- token_reuse_rejected true (token_reuse_across_tasks)

## Lessons
- Named Career Journal is source of truth for Sai's public-vs-confidential wording.
- For this *generic* framework spec, FFIEC/CRI/FINRA/FDIC are requested regulatory baselines, not claims about his employer.
- KPI section must use formulas and labeled planning bands, not fabricated "hours saved" results.
- PoC metrics must be computed from fixture runs and labeled SYNTHETIC.
- Canvas Pill cannot take React `key` in this SDK — unroll or wrap in span.
- Cloud agent: feature branches must match `cursor/<descriptive-name>-96ac`; preferred base is `cursor/acbn-synthetic-poc`.
- Dual-control demo should attempt the unbound task *before* consume so the artefact shows `token_reuse_across_tasks`, not only `token_already_consumed`.
